package utils.mapper;

import productos.Cuenta;

import java.sql.ResultSet;

public class CuentaMapper implements baseMapper<Cuenta>{
    @Override
    public Cuenta toModel(ResultSet s) {
        return null;
    }
}
