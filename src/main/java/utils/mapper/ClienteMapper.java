package utils.mapper;

import personas.Cliente;

import java.sql.ResultSet;

public class ClienteMapper implements baseMapper<Cliente>{

    @Override
    public Cliente toModel(ResultSet s) {
        return null;
    }
}
